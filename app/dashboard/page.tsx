import { FolderBrowser } from "@/components/folder-browser";

/** Staff dashboard: folder library + global search (no admin tools on this screen). */
export default function DashboardPage() {
  return <FolderBrowser />;
}
